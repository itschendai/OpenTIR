Named single-pose key positions recorded by `record_robot_waypoints.py`.

Use commands such as `key Tray`, `key Intermediate`, or `key Vise` while the
recorder is running. Each command writes one JSON file here and updates
`index.json`.

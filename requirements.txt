# OpenTIR project dependencies — tested with Python 3.12 (works on 3.10+).
# Rebuild the environment with:
#   python -m venv .venv
#   source .venv/bin/activate        # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt

# --- Flexiv robot SDK ---
flexivrdk
spdlog

# --- Vision / camera ---
opencv-python
pyrealsense2
numpy
matplotlib

# --- Hardware I/O ---
pyserial>=3.5

# --- Config / data ---
pyyaml>=6.0

# --- GUI ---
flask>=3.0

# --- Testing ---
pytest>=7.0
